# Dashboard Screenshots

This directory keeps only a small, compressed representative sample for docs/review.

Tracked files:
- `sample/*.webp`

Raw timestamped captures from `scripts/capture-dashboard.ts` are intentionally gitignored.
They remain local for manual inspection unless explicitly moved/added.
