{
  "name": "dotclaude-skills-mcp",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^7"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "yaml": "^2.9.0",
    "zod": "^4.5.4"
  },
  "scripts": {
    "serve": "bun stdio.ts",
    "test": "bun test conformance",
    "typecheck": "tsc --noEmit",
    "build": "bun worker/build.ts"
  },
  "engines": {
    "bun": ">=1.4.0"
  }
}
